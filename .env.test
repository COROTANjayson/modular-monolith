# Test Database Configuration
DATABASE_URL="postgresql://testuser:testpassword@localhost:5433/modular_monolith_test?schema=public"
DIRECT_URL="postgresql://testuser:testpassword@localhost:5433/modular_monolith_test?schema=public"

# JWT Configuration (use different secrets for testing)
JWT_ACCESS_TOKEN_SECRET="test-access-secret-key-for-integration-tests"
JWT_REFRESH_TOKEN_SECRET="test-refresh-secret-key-for-integration-tests"
ACCESS_TOKEN_EXPIRES_IN="1d"
REFRESH_TOKEN_EXPIRES_IN="7d"

# Disable Redis for tests (queues will run synchronously)
REDIS_URL=""

# Supabase (not used in tests, but required by config)
SUPABASE_URL="https://test.supabase.co"
SUPABASE_KEY="test-key"

# Server Configuration
PORT=3001
NODE_ENV="test"

# Cookie Settings
REFRESH_COOKIE_NAME="refreshToken"
CSRF_COOKIE_NAME="csrf_token"
COOKIE_DOMAIN="localhost"
COOKIE_SECURE="false"
COOKIE_SAME_SITE="lax"

# Client URL
CLIENT_URL="http://localhost:3000"

# Email Configuration (disabled for tests)
RESEND_API_KEY=""
RESEND_SENDER_EMAIL="test@example.com"
EMAIL_QUEUE_RATE_LIMIT=10
EMAIL_QUEUE_RATE_DURATION=60000
