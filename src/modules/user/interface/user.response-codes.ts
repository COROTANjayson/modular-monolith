export const USER_SUCCESS_CODES = {
  USER_DATA_FETCHED: "USER_DATA_FETCHED",
  USER_DATA_UPDATED: "USER_DATA_UPDATED",
} as const;

// Currently no specific user error codes in the original file, but creating the object for consistency if needed later
export const USER_ERROR_CODES = {} as const;
